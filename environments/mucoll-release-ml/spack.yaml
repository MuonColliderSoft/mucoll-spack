spack:
  packages:
    all:
      target: [x86_64, aarch64]
      variants: 
        - build_type=RelWithDebInfo
        - 'cxxstd=20'
      buildable: true
      providers:
        gl: [mesa]
        glu: [mesa]

  concretizer:
    unify: true
    targets:
      host_compatible: true

  view: false

  include:
  - ../mucoll-common/packages.yaml

  specs:
  - mucoll-stack@full_gaudi_test+devtools+pytools+ml
