name: Concretize Stack

on: [pull_request]

jobs:
  concretize:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu24]
        target: [layered]
    uses: ./.github/workflows/concretize-template.yaml
    with:
      target: ${{ matrix.target }}
      os: ${{ matrix.os }}
